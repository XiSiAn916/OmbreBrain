# Ombre Brain 同步、备份与迁移方案

## 为什么同步/备份重要？

你问了个很好的问题。Ombre Brain 的记忆是你和 AI 共同积累的"关系记忆"——一起聊过的天、笑过的梗、建立起来的默契。

**不能因为换个手机就没了。**

## 三层备份体系

```
第一层：本地数据库
  格式: ombre_brain.db (SQLite + AES 加密)
  位置: App 内部存储空间（Android data 目录）
  特点: 最快，零网络，App 启动即用

第二层：.ombre 加密备份文件（手动/定时）
  格式: ZIP 压缩 + AES-256 加密
  位置: /sdcard/Download/OmbreBrain/ 或 任意你选的位置
  特点: 可传输、可存云盘、换设备时导入

第三层：Operit 记忆同步（自动，热备份）
  协议: Operit Memory API（query_memory / get_memory_by_title）
  内容: 选择性同步最重要的记忆（非全部，避免刷屏）
  特点: 换设备登录同一 Operit 账号即可恢复关键记忆
```

## 换设备完整迁移流程（你担心的场景）

### 场景：旧手机 → 新手机

```
旧手机:
┌────────────────────────────────────┐
│ 1. 打开 Ombre Brain → 设置 → 备份  │
│ 2. 点击「导出备份」                  │
│ 3. 输入你设定的备份密码              │
│ 4. 生成 .ombre 文件                 │
│ 5. 文件保存到 /sdcard/Download/     │
│ 6. (可选) 通过 Operit 同步也已备份   │
└──────────────────────┬─────────────┘
                       ↓
      把 .ombre 文件传到新手机
      (蓝牙 / 微信 / 云盘 / USB)
                       ↓
新手机:
┌────────────────────────────────────┐
│ 1. 安装 Ombre Brain App             │
│ 2. 首次启动 → 检测到备份文件        │
│ 3. 选择「导入备份」                 │
│ 4. 输入备份密码                     │
│ 5. 自动解密 + 恢复所有记忆          │
│ 6. 同时连接 Operit 记忆补全同步     │
│ ✅ 搞定！和旧手机一模一样            │
└────────────────────────────────────┘
```

### 你问的「用 Operit GitHub 同步」方案

如果你新手机也登录了同一个 Operit 账号（配置了 GitHub 同步），那么：
- **旧手机**上 Ombre Brain 已定期同步到 Operit 记忆
- **新手机**装好 Ombre Brain App → **不需要手动导入备份**
- 启动时检测到 Operit 账号 → 自动从 Operit 记忆拉取恢复
- `.ombre` 备份文件作为**保险**，以防网络同步不及时

> **最佳实践：两者都做**
> `.ombre` 备份是"我把记忆装在 U 盘里揣兜里"
> Operit 同步是"云上自动备份，到哪都能取"
> 一起用，无论什么情况都丢不了。

## 备份文件格式

```
ombre-brain-backup_2026-07-29.ombre
├── [加密头] 8 bytes (魔数: 0x4F4D4252 = "OMBR")
├── [版本号] 2 bytes
├── [时间戳] 8 bytes (Unix ms)
├── [加密数据区]
│   ├── memories.json (AES-256-GCM 加密)
│   ├── flashes.json (AES-256-GCM 加密)
│   ├── habits.json (AES-256-GCM 加密)
│   └── config.json (AES-256-GCM 加密)
└── [MAC 签名] 16 bytes (完整性校验)
```

## 自动定时备份

| 触发时机 | 行为 |
|----------|------|
| 每天凌晨（App 处于前台） | 自动创建 .ombre 增量备份 |
| 每新增 50 条记忆 | 触发一次备份 |
| 重要度 > 0.9 的记忆首次生成 | 立即同步到 Operit 记忆 |
| 换设备检测（首次启动无本地数据） | 提示导入或从 Operit 恢复 |

## Operit 同步策略

```
Ombre Brain 本地                     Operit 记忆
┌──────────────┐                  ┌──────────────┐
│  重要记忆     │ ──── 同步 ────→  │  记忆条目      │
│  (重要度>0.6) │                  │  (可在Operit)  │
│              │                  │   搜索到)       │
│  闪现想法     │ ── (可选) ──→   │  灵感笔记       │
│  固化习惯     │ ──── 同步 ────→  │  核心设定       │
│  沉睡记忆     │ ←─── 不同步      │  (只在本地)    │
└──────────────┘                  └──────────────┘
```

**不同步沉睡记忆的原因**：避免 Operit 记忆被低频数据刷屏。你换设备时，沉睡记忆通过 `.ombre` 完整恢复。

## 冲突解决

当本地和 Operit 同一记忆出现版本不一致时：

```
记忆 A 在本地版本: 2026-07-29 06:00
记忆 A 在 Operit 版本: 2026-07-29 07:30 (新)
→ 取时间戳更新的版本

记忆 A 在本地版本: 2026-07-29 08:00 (新)
记忆 A 在 Operit 版本: 2026-07-29 07:30
→ 取时间戳更新的版本

两边同时修改（罕见）：
→ 取重要度更高的版本
→ 如重要度相同，合并内容
```