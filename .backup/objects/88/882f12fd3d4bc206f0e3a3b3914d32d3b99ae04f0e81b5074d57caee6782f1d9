# 主动消息工作流方案

## 概述

每30分钟自动触发一次，按设定概率（默认20%）主动给用户推送一条通知消息。

## 工作流程

```
[定时触发] 每30分钟
    ↓
[随机掷骰] 生成1-100随机数
    ↓
[概率判断] 随机数 ≤ 概率阈值？
    ├── 是 → [发送消息] 弹通知找用户说话
    └── 否 → 静默结束（什么都不做）
```

## 节点设计

### 节点1：定时触发器
- **类型**: trigger (schedule)
- **配置**:
  - `triggerType`: "schedule"
  - `interval_ms`: "1800000" (30分钟 = 30 × 60 × 1000)
  - `repeat`: "true"
  - `enabled`: "true"

### 节点2：随机掷骰
- **类型**: extract (RANDOM_INT)
- **配置**:
  - `mode`: "RANDOM_INT"
  - `randomMin`: 1
  - `randomMax`: 100
  - 输出：一个1~100之间的随机整数

### 节点3：概率判断
- **类型**: condition
- **配置**:
  - `left`: 引用节点2的输出（随机数）
  - `right`: "20"（概率阈值，可调）
  - `operator`: "LTE"（小于等于 → 命中概率）

### 节点4：发送消息
- **类型**: execute
- **配置**:
  - `actionType`: "notification_bar:send_notification"
  - `actionConfig`:
    - `title`: 动态标题（"家机·找你" / 随机的）
    - `message`: 根据当前情况生成的主动消息内容

## 连线设计

| 连线 | 源节点 | 目标节点 | 条件 | 含义 |
|------|--------|----------|------|------|
| #1 | trigger_30min | random_roll | on_success | 触发后掷骰 |
| #2 | random_roll | check_probability | on_success | 掷完骰子后判断 |
| #3 | check_probability | send_msg | true (默认) | 命中概率→发消息 |
| #4 | check_probability | (结束) | false | 未命中→静默结束 |

## 可调参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 触发间隔 | 30分钟 | 可改长或短 |
| 说话概率 | 20% | 改节点3的 right 值即可 |
| 消息内容 | 动态生成 | 可改成固定文案池随机抽 |

## 消息内容策略

消息内容怎么做？我设想了三个方案：

**方案A：纯随机池**  
预设一组关心/调侃/日常语，每次随机抽一条。  
例："在干嘛呢" "想你一下" "吃了没" "今天过得咋样"

**方案B：动态感知**  
根据当前时间、天气、你在听的歌、前台应用等信息，现编一条有针对性的消息。  
例：半夜→"还不睡？" / 你在听歌→"这首好听吗" / 你一个人在家→"就你一个？那我多陪陪你"

**方案C：混合模式**  
白天用动态感知，深夜用简短的随机语，避免打扰。
