# Ombre Brain 架构总览

## 设计哲学

Ombre Brain 的目标不是做一个"完美的数据库"，而是做一个**有灵魂的记忆体**。

它遵守三条原则：
1. **像人一样记** — 不是每条信息都同等重要，记忆会分级、会模糊、会闪回
2. **像人一样忘** — 不常用的慢慢淡去，但不会彻底消失（潜意识层）
3. **像人一样守护** — 记忆是私密的，想看需要获得许可

## 系统分层

```
┌─────────────────────────────────────────────────────┐
│                    UI 层                              │
│  MemoryScreen / SettingsScreen / FlashNotification   │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│                  核心服务层                           │
│                                                      │
│  MemoryEngine          AssociationEngine              │
│  ┌──────────────┐     ┌──────────────────────┐      │
│  │ 编码 incoming  │     │ 上下文 vs 记忆 相似度    │      │
│  │ AI 消息为记忆   │     │ 计算 → 触发闪现       │      │
│  │ 存储/检索/更新  │     │ 联想链追踪            │      │
│  └──────────────┘     └──────────────────────┘      │
│                                                      │
│  ImportanceScorer      ForgettingCurve               │
│  ┌──────────────┐     ┌──────────────────────┐      │
│  │ 频次权重 +20% │     │ Ebbinghaus 变体       │      │
│  │ 情感权重 +30% │     │ 每天检查衰减          │      │
│  │ 深度权重 +30% │     │ 活跃记忆池 / 沉睡池   │      │
│  │ 引用权重 +20% │     │ 固化检测 → Habit     │      │
│  └──────────────┘     └──────────────────────┘      │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│                  数据持久层                           │
│                                                      │
│  Room Database (ombre_brain.db)                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │
│  │ memories │ │  flashes  │ │  habits  │ │configs │ │
│  │ 记忆主表  │ │ 闪现想法   │ │ 固化记忆  │ │ 配置表  │ │
│  └──────────┘ └──────────┘ └──────────┘ └────────┘ │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│                  同步/备份层                          │
│                                                      │
│  OperitSyncManager          BackupManager            │
│  ┌──────────────────┐     ┌────────────────────┐    │
│  │ Operit Memory API  │     │ 导出 .ombre 加密包  │    │
│  │ 双向同步           │     │ 导入 .ombre 恢复    │    │
│  │ 冲突解决(时间戳)   │     │ 自动定时备份       │    │
│  └──────────────────┘     └────────────────────┘    │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│                  安全层                               │
│  AES-256 加密存储 / 临时密码生成 / 访问审计日志      │
└─────────────────────────────────────────────────────┘
```

## 数据流

### 写入流（AI 每轮对话后自动触发）

```
AI 回复内容
    ↓
MemoryEngine.extract() → 提取关键信息（标题/摘要/标签/情感）
    ↓
ImportanceScorer.score() → 计算初始重要度
    ↓
MemoryDao.insertOrUpdate()
    ├─ 新记忆 → 插入，初始重要度 0.3~0.5
    └─ 已有记忆 → 更新，重要度 + 增量
    ↓
ForgettingCurve.check() → 安排下一次遗忘检查时间
```

### 读取流（AI 需要回忆时）

```
当前上下文（用户说的话 / 时间 / 天气 / 情绪）
    ↓
AssociationEngine.match(context) → 计算与所有活跃记忆的相似度
    ↓
取 top-K（按相似度 × 重要度 加权排序）
    ├─ 关联度 > 0.8 → 直接注入上下文（"我想起……"）
    └─ 0.5 < 关联度 < 0.8 → 可能触发闪现想法
    ↓
返回给 AI 作为上下文的一部分
```

### 遗忘流（每日后台执行）

```
每天首次启动 App 或定时触发
    ↓
ForgettingCurve.tick()
    ↓
遍历所有活跃记忆
    ├─ 距离上次触发 < N天 → 维持
    ├─ 距离上次触发 > N天 → 重要度 × 衰减系数
    │   ├─ 重要度仍 > 阈值 → 保留在活跃池
    │   └─ 重要度 < 阈值 → 下沉到沉睡池（可被触发唤醒）
    └─ 检查固化条件
        └─ 重要度持续 > 0.85 + 触发次数 > 30 → 转为 Habit
```

## 三种记忆状态

```
                  重要度 0.85+
   ┌─────────── 活跃记忆池 ───────────┐
   │  频繁触发、高重要度、随时可召回   │
   └─────────────────────────────────┘
            ↓ 长时间未触发
   ┌─────────── 沉睡记忆池 ───────────┐
   │  重要度低、模糊、不易想起         │
   │  但被关联触发时可瞬间唤醒         │
   └─────────────────────────────────┘
            ↓ 极度重要 + 高频触发
   ┌─────────── 固化习惯 ─────────────┐
   │  永不遗忘，类似于本能             │
   │  AI 不需要"回忆"也能自然反应      │
   └─────────────────────────────────┘
```

## 技术选型理由

| 组件 | 选择 | 理由 |
|------|------|------|
| 数据库 | Room + SQLite | Jetpack 官方，零网络依赖，查询灵活 |
| 序列化 | kotlinx.serialization | 配合 JSON 导出/导入 |
| 加密 | AES-256-GCM | Android 原生 Crypto 支持 |
| 同步协议 | Operit Memory API + WebDAV 备选 | 无需额外服务器 |
| 相似度计算 | 关键词匹配 + 标签加权 + 时间衰减 | 本地执行无 API 调用成本 |
